# 注意事项

脱敏不是加密，它的保证比听起来弱。以下是使用 privllm 前必须认真读的完整清单。README 里只放了最要紧的一条（还原不完整时的应对），详细说明都在这里。

## 1. 自由文本的语义完全不受保护

这是最根本的限制。看 demo 里的实际输出：

```
原始：我们计划以 3.2 亿元收购星海动力集团的电池业务……
发出：我们计划以 3.2 亿元收购云启材料的电池业务……
```

姓名和号码换掉了，但服务方仍然完整读得出：这是一起 3.2 亿元的并购、标的是电池业务、交易正在进行中。

**如果"内容本身即机密"，实体替换帮不了你。** 这只能靠本地推理（直接跑本地模型，或部署在 TEE 里）。`privllm` 解决的是"内容可以外发，但里面的个人身份信息不能外发"这一类问题。

## 2. 假名可被字典攻击

确定性是特性也是弱点。拿到密钥的人可以对假名做字典攻击——用一个常见中文姓名表试一遍，几秒钟就能反查出真名。

所以密钥的保管就是全部的安全性。数据在服务方那边也是同理：如果服务方知道某人是"某公司的财务总监"，他能猜出假名对应的真人。

## 3. 假名被改写就还原不回来

模型可能把"青丘市扶摇路70号"简写成"青丘市"，或者把"云启材料"写成"云启"。还原是精确字符串匹配，改写过就对不上，`restore()` 会告警但恢复不了。

没有一劳永逸的解法：`⟦⟧` 标记会被 gemma4:e2b / qwen3:8b 当格式吞掉，system 指令对模型是软约束。当前默认做法是让假名自带前缀——人名是「翎羽·刘瑶」这种名字样标记，机构名是「假名·天成材料」——模型能自己判断这是假名。但也不是保证，模型仍可能改写（比如把「翎羽·刘瑶」写成「刘瑶」，或把「云启材料」缩成「云启」）。

## 4. 自然人名碰撞（已用名字样标记缓解）

如果模型自己写出了一个和假名同名的真人——映射表里"刘瑶"对应"张伟"，而模型回复里恰好有个真实的"刘瑶"——裸串匹配会把她也换成"张伟"。

名字样标记就是为了把这个概率压下去：`翎羽·刘瑶` 里的「翎羽」现实中不是人名，所以只要模型保留前缀，`翎羽·刘瑶` 只可能是我们自己放进去的，模型自己写出的真人"刘瑶"不会被误伤。实测 gemma4:e2b 对**名字样标记保留良好（3/3）**，对机构名的「假名·」前缀也保留良好，英文人名 `Sparrow Smith` 的名词性标记同样保留良好（3/3）；但中文人名偶尔仍会把前缀剥掉——前缀一掉就退回裸串匹配，碰撞风险回归，`restore()` 会明确告警而不是静默降级。这是"可读假名"换来的残余风险。

## 5. 英文的姓氏指代归并是有歧义的

「John Smith」后文单独出现的「Smith」会被归并到「John Smith」——这在业务文本里通常正确，但若文本里有「John Smith」和「Alice Smith」两个人，两个「Smith」都会归并到先出现的那个全名。另外**名字（first name）引用不归并**：「John」长度比 0.4 < 0.5，不会被当成「John Smith」的指代（英文里只用名字指代确实有歧义，这是保守选择）。英文人名假名保留真实姓氏（`John Smith → Sparrow Smith`），所以模型只写「Smith」时无需还原——姓氏根本没换过；只有写 given name「Sparrow」时才无法还原，与上面的「名字引用不归并」是同一层保守选择。正则只覆盖美式格式（`+1` 国家码、SSN）；英式邮编、其它国家号码需要自己往 `PATTERNS` 里加，国际格式的兜底靠 LLM 层 + 回扫补漏。

## 6. 邮箱大小写被规范化、用户名/密码靠语义识别

邮箱脱敏时统一 lower（`John.Doe@Example.com` 与 `john.doe@example.com` 是同一个邮箱），还原回来全是小写——邮箱大小写不敏感，这通常无害，但严格逐字往返做不到。用户名/密码没有可靠正则，只能靠模型的语义识别（"用户名是 xxx"、"密码: xxx"），召回率取决于模型；它们没有缩写指代概念，不做回扫和别名归并——两个用户 `admin` 与 `admin1` 不会被误并，但代价是模型若把用户名/密码只写一半（比如把 `alice_wang` 写成 `alice`），就还原不回来。

## 7. 这是原型，不是生产系统

上生产前至少要补齐：

- **密钥轮换**。HKDF 的 `info` 里带了 `v1`，换版本等于所有历史映射失效，是全有全无的。真正的轮换需要多版本并存。
- **`chmod` 在 Windows 上基本无效**。密钥文件的保护靠目录 ACL，不是代码里那行 `chmod`。
- **HTTP 明文**。示例连的是 `http://` 的内网 Ollama。跨公网必须上 TLS，否则脱敏文本连同中间人可截获的一切一起裸奔。
- **`PrivacyProxy` 不是线程安全的**。每个会话一个实例。
