# 核心设计

本文档面向维护者与贡献者，讲清楚 privllm 的内部设计决策和实现细节——包括那些"使用者不需要知道"的开发事项。使用者只需读 [README.md](README.md)。

## FF1 保格式加密（[privllm/ff1.py](privllm/ff1.py)）

结构化字段用 NIST SP 800-38G 的 FF1。它是一个基于 AES 的 10 轮 Feistel 网络，在任意进制的字母表上工作——所以"加密后还是 11 位数字"是构造上保证的，不是靠截断或补位凑出来的。

实现对照标准里的 9 组官方向量全部通过，包括 radix=10 和 radix=36、AES-128/192/256 三种密钥长度、带 tweak 和不带 tweak。

两个容易写错的地方，都在代码注释里标了：

- 标准里的 `[x]^s` 指的是"整数 x 按大端编码成 s 个字节"，不是"把 x 重复 s 次"。
- 解密不是"加密然后把加法换成减法"。轮序要反过来，Q 要用 A 构造，交换方向也要反。

**FF3 没有实现**，是故意的：Durak & Vaudenay 在 2017 年发表的攻击能在几小时内恢复 FF3 的完整密钥，NIST 已经把它撤回了。

同一密钥配不同的 tweak，把手机号、身份证、卡号、邮箱隔到互不相通的定义域里。否则同一串数字在不同字段间会算出同样的密文，直接泄露"这两个字段值相同"。

FF1 有个定义域下限：`radix^minlen >= 1,000,000`。10 进制下就是至少 6 位。太短的输入（比如 5 位验证码）会被拒绝，因为它在一个小到可以暴力枚举的空间里根本不提供安全性。邮箱用的是 radix=36 的字母表（`0-9a-z`），下限是 4 个可加密字符。

## 可读假名（[privllm/pseudo.py](privllm/pseudo.py)）

人名、公司名这类不受格式约束的实体，换成由 HMAC-SHA256 派生出来的**可读**假名：真实姓氏表 + 名字表 + 公司前后缀。同一个实体永远得到同一个假名。

假名前面带一个前缀，让替身**自带**假名身份——模型一看就知道这不是真名，而不是靠额外的 system 指令或括号去提示。前缀按实测保留行为来选：

- **中文人名**用**名字样标记**（`MARKERS`）：`张伟 → 翎羽·刘瑶`。这些 2 字诗性词（翎羽/弋星/星澜/…）长得像人名成分，模型会把它当一个名字原样保留；而 `假名·刘瑶` 里的「假名」是词典词，会被模型当称谓剥掉（只写「刘瑶」）。同时它们现实中没人叫，撞真名概率≈0。
- **中文机构名**用**词典词前缀**「假名·」：`星海动力 → 假名·天成材料`，实测保留良好。
- **英文人名**用**名词性标记**（`EN_MARKERS`）只换名字、保留真实姓氏：`John Smith → Sparrow Smith`。`alias ` 这个词典词对人和机构都被模型当 "aka" 剥掉（实测 0/3），而 `Sparrow` 这种 nature-name 风格的词当 given name 用会原样保留（实测 3/3）。保留姓氏是因为姓氏本身不敏感——模型只写「Smith」时无需还原。**英文机构名**直接去掉 `alias `——`Acme Corporation → Titan Dynamics`，前缀本身已够有辨识度。
- **中性实体**（用户名/密码/项目代号/地址）用"类真实形态"的假名：`密码 → Quc1rOUYkd`、`项目代号 → 惊鸿`、`地址 → 青丘市扶摇路70号`，让模型当一个整体保留而不是拆成"标签+值"。这是后来改的：早期用 `类别-随机串`（「地点-PXBV」「用户-3C75」），实测 gemma4:e2b 会把「地点-PXBV」剥成「PXBV」——「地点」被当成字段标签、「-」是键值分隔符、随机串是值，模型自然拆开。改成"类真实形态"后模型把 `青丘市扶摇路70号` 当一个完整地址原样保留。语言跟随整段文本的脚本。

结构化字段（手机号/证件号/邮箱）走保格式加密，密文形态上就是数字/邮箱、没有真假难辨的问题，所以不套前缀。

为什么不做成 `PERSON_a3f2` 这种 token：模型看到 `PERSON_a3f2` 时丢掉的不只是读音，还有"这是个人名、是中文名、大概几个字"这些线索，而推理依赖它们。实测可读假名能让模型保持正常的称呼和指代行为。

为什么无状态：假名由 `HMAC(key, kind ‖ normalize(entity) ‖ counter)` 算出，不依赖任何已建立的映射表。分布式部署、进程重启、跨会话，只要密钥一致就一致。

## 一个主密钥 → 三支子密钥（[privllm/keys.py](privllm/keys.py)）

HKDF-SHA256，`info` 分别是 `privllm/v1/ff1`、`/pseudo`、`/vault`。

这不是"顺手分一下"的洁癖，而是必需的：

| 子密钥 | 必须的性质 | 原因 |
|---|---|---|
| `ff1` | **确定性** | 同一个手机号每次必须加密成同一个值，否则模型无法指代消解 |
| `pseudo` | **确定性** | 同上 |
| `vault` | **绝不确定** | 必须用随机 nonce 的 AES-GCM，否则相同明文产生相同密文，映射表本身就泄密 |

前两个要求确定、第三个要求不确定——这是两个相反的要求，共用一支密钥必然违反其中一个。

## 两级实体识别（[privllm/ner.py](privllm/ner.py)）

手机号、身份证、银行卡、邮箱有确定形态，正则又快又准，没必要动用模型。人名、公司名、地址、项目代号、用户名、密码在中文里没有可靠的正则，只能用模型。

两者冲突时正则优先——形态明确的赢。

**回扫补漏**：NER 召回不完美是常态。模型报了"星海动力集团"，却漏了后文的"星海动力"——对脱敏来说漏检不是"少脱一点"，而是那段文字以明文原样发出。所以拿已知实体的前缀/后缀去全文回扫，把漏网的缩写找回来。

宁可过度脱敏也不能漏。这是隐私工具唯一正确的偏向。

**别名归并**：「星海动力」和「星海动力集团」是同一家公司。不归并就会派生出两个假名，模型会当成两家公司在谈——这比不脱敏还糟，因为它给出了错误的事实。

归并规则保守（仅当前缀匹配且长度比 ≥ 0.5），已知会误判「中国」与「中国银行」，代码里固化了这个行为并写了测试。

用户名/密码/邮箱没有"缩写指代"概念——把密码「abc123」切成「abc」去全文回扫只会误伤别的数字，把用户名「admin」与「admin1」前缀归并会错并两个账号。所以这三类**不做回扫、不做别名归并**，只按 LLM/正则报出的原文精确替换。

## 失败关闭

实体识别依赖模型。模型挂了怎么办？

若继续把原文发给服务方，脱敏层就成了摆设，而且没人会发现。所以默认 `on_llm_failure="raise"`——直接失败。

想降级也可以，但必须显式选择，而且降级会产生明确告警：

```python
red = proxy.redact(text, use_llm=True, on_ner_failure="regex_only")
# 手机号/身份证仍受保护，但人名、公司名会被原样放过——告警里会写清楚
```

## 单遍还原

还原用一次 `re.sub` 扫描，不能用连续的 `str.replace`。

如果映射表里有 `{李白 → 王强, 王强 → 刘瑶}`，逐条替换会把「李白」变成「刘瑶」——把 B 的话安到 A 头上。`re.sub` 一次性扫描原文，替换结果不再重扫，从构造上消除这个问题。长键优先，避免短键吃掉长键的前缀。

## 界定标记（`mark`）

`mark="⟦{}⟧"` 给占位符外套一对界定符，让匹配变成精确的——`⟦刘瑶⟧` 只可能是我们自己放进去的，模型自己写出的同名人名不会被误认成映射表里的实体。

但实测 **gemma4:e2b 和 qwen3:8b 都会把这对方括号当格式清理掉**。两个模型都不保留标记，标记就只剩复杂度，还给出一种虚假的安全感。所以默认关闭；换成明确会保留结构化边界的模型（Claude/GPT 一级）时才值得打开。

`restore` 对这两种信号（mark 界定符、假名前缀）都有降级路径：标记或前缀被模型剥掉时，退回裸串匹配并明确告警，绝不静默失败。
